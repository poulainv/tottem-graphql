# Migration `20191222015616-section-name-optional`

This migration has been generated by Vincent Poulain at 12/22/2019, 1:56:16 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Section" DROP COLUMN "name",
ADD COLUMN "name" text   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191222015303-add-updated-at..20191222015616-section-name-optional
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model User {
   id          String       @default(cuid()) @id
@@ -29,9 +29,9 @@
   id          String       @default(cuid()) @id
   createdAt   DateTime     @default(now())
   updatedAt   DateTime     @updatedAt
   slug        String
-  name        String       @default("New space")
+  name        String?
   index       Int          @default(0)
   collections Collection[]
   owner       User
   isExpanded  Boolean      @default(false)
```

## Photon Usage

You can use a specific Photon built for this migration (20191222015616-section-name-optional)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191222015616-section-name-optional'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
