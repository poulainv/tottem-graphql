# Migration `20191111183740-remove-section-unicity`

This migration has been generated by Vincent Poulain at 11/11/2019, 6:37:40 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP INDEX "public"."Section.slug"
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191111182940-first-commit..20191111183740-remove-section-unicity
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model User {
   id          String       @default(cuid()) @id
@@ -22,9 +22,9 @@
 model Section {
   id          String       @default(cuid()) @id
   createdAt   DateTime     @default(now())
-  slug        String       @unique
+  slug        String       
   name        String
   index       Int          @default(0)
   collections Collection[]
   owner       User
```

## Photon Usage

You can use a specific Photon built for this migration (20191111183740-remove-section-unicity)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191111183740-remove-section-unicity'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
