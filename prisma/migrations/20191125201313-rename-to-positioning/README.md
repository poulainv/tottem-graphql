# Migration `20191125201313-rename-to-positioning`

This migration has been generated by Vincent Poulain at 11/25/2019, 8:13:13 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Item" DROP COLUMN "index",
ADD COLUMN "position" integer NOT NULL DEFAULT 0 ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191125184628-add-index-item..20191125201313-rename-to-positioning
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model User {
   id          String       @default(cuid()) @id
@@ -50,9 +50,9 @@
   id          String      @default(cuid()) @id
   createdAt   DateTime    @default(now())
   updatedAt   DateTime    @updatedAt
   isArchived  Boolean     @default(false)
-  index       Int         @default(0)
+  position    Int         @default(0)
   title       String
   provider    String?
   author      String?
   type        ItemType
```

## Photon Usage

You can use a specific Photon built for this migration (20191125201313-rename-to-positioning)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191125201313-rename-to-positioning'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
