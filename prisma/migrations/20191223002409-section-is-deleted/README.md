# Migration `20191223002409-section-is-deleted`

This migration has been generated by Vincent Poulain at 12/23/2019, 12:24:09 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Section" ADD COLUMN "isDeleted" boolean NOT NULL DEFAULT false ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191222145827-collection-name-optional..20191223002409-section-is-deleted
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model User {
   id          String       @default(cuid()) @id
@@ -34,8 +34,9 @@
   index       Int          @default(0)
   collections Collection[]
   owner       User
   isExpanded  Boolean      @default(false)
+  isDeleted   Boolean      @default(false)
 }
 model Collection {
   id        String   @id @default(cuid())
```

## Photon Usage

You can use a specific Photon built for this migration (20191223002409-section-is-deleted)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191223002409-section-is-deleted'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
