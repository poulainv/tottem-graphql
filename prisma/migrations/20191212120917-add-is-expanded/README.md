# Migration `20191212120917-add-is-expanded`

This migration has been generated by cdeon at 12/12/2019, 12:09:17 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."User" DROP COLUMN "test";

ALTER TABLE "public"."Section" ADD COLUMN "isExpanded" boolean NOT NULL DEFAULT false ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191203154422-test..20191212120917-add-is-expanded
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model User {
   id          String       @default(cuid()) @id
@@ -20,9 +20,8 @@
   mail        String?
   youtube     String?
   website     String?
   label       String?
-  test          String?
   sections    Section[]
   collections Collection[]
 }
@@ -33,8 +32,9 @@
   name        String
   index       Int          @default(0)
   collections Collection[]
   owner       User
+  isExpanded  Boolean       @default(false)
 }
 model Collection {
   id        String   @id @default(cuid())
```

## Photon Usage

You can use a specific Photon built for this migration (20191212120917-add-is-expanded)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191212120917-add-is-expanded'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
