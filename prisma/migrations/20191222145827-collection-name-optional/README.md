# Migration `20191222145827-collection-name-optional`

This migration has been generated by Vincent Poulain at 12/22/2019, 2:58:27 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Collection" DROP COLUMN "name",
ADD COLUMN "name" text   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191222015616-section-name-optional..20191222145827-collection-name-optional
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model User {
   id          String       @default(cuid()) @id
@@ -40,9 +40,9 @@
 model Collection {
   id        String   @id @default(cuid())
   createdAt DateTime @default(now())
   slug      String   @unique
-  name      String
+  name      String?
   isDeleted Boolean  @default(false)
   detail    String?
   items     Item[]
   section   Section
```

## Photon Usage

You can use a specific Photon built for this migration (20191222145827-collection-name-optional)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191222145827-collection-name-optional'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
