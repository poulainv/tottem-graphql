# Migration `20191222015303-add-updated-at`

This migration has been generated by Vincent Poulain at 12/22/2019, 1:53:03 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Section" ADD COLUMN "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191219121020-first-commit..20191222015303-add-updated-at
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model User {
   id          String       @default(cuid()) @id
@@ -27,10 +27,11 @@
 model Section {
   id          String       @default(cuid()) @id
   createdAt   DateTime     @default(now())
+  updatedAt   DateTime     @updatedAt
   slug        String
-  name        String
+  name        String       @default("New space")
   index       Int          @default(0)
   collections Collection[]
   owner       User
   isExpanded  Boolean      @default(false)
```

## Photon Usage

You can use a specific Photon built for this migration (20191222015303-add-updated-at)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191222015303-add-updated-at'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
