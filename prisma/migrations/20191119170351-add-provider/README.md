# Migration `20191119170351-add-provider`

This migration has been generated by Vincent Poulain at 11/19/2019, 5:03:51 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Item" ADD COLUMN "provider" text   ;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191115171740-first..20191119170351-add-provider
--- datamodel.dml
+++ datamodel.dml
@@ -3,14 +3,14 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model User {
   id          String       @default(cuid()) @id
-  authUserId  String?       @unique
+  authUserId  String?      @unique
   slug        String       @unique
   createdAt   DateTime     @default(now())
   firstname   String
   pictureUrl  String
@@ -27,9 +27,9 @@
 model Section {
   id          String       @default(cuid()) @id
   createdAt   DateTime     @default(now())
-  slug        String
+  slug        String       
   name        String
   index       Int          @default(0)
   collections Collection[]
   owner       User
@@ -50,8 +50,9 @@
   id          String      @default(cuid()) @id
   createdAt   DateTime    @default(now())
   updatedAt   DateTime    @updatedAt
   title       String
+  provider    String?
   author      String?
   type        ItemType
   productUrl  String?
   imageUrl    String?
```

## Photon Usage

You can use a specific Photon built for this migration (20191119170351-add-provider)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191119170351-add-provider'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
