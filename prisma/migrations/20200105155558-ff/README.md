# Migration `20200105155558-ff`

This migration has been generated by Vincent Poulain at 1/5/2020, 3:55:58 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Item" DROP COLUMN "user",
ADD COLUMN "inboxOwner" text   REFERENCES "public"."User"("id") ON DELETE SET NULL;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20200104181841-first-commit..20200105155558-ff
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model User {
   id           String       @default(cuid()) @id
@@ -21,10 +21,10 @@
   youtube      String?
   website      String?
   label        String?
   sections     Section[]
+  collections  Collection[]
   inboxedItems Item[]
-  collections  Collection[]
 }
 model Section {
   id          String       @default(cuid()) @id
@@ -67,8 +67,9 @@
   description String?
   comment     String?
   collection  Collection?
   meta        String?     @default("{}")
+  inboxOwner  User
 }
 enum ItemType {
```

## Photon Usage

You can use a specific Photon built for this migration (20200105155558-ff)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20200105155558-ff'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
